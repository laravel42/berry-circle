-- Reverses 185. Dropping the table forgets a cache of GitHub's answer: the
-- installations stay in github_installations, and a refresh rebuilds this.
DROP TABLE IF EXISTS github_granted_repositories;
