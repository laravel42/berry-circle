// Package db documents Berry's sqlc inputs.
package db

// The current sqlc CLI transitively depends on an MPL-2.0 MySQL driver, so it
// is not retained as a Go tool under Berry's permissive-only dependency rule.
// See the server README before regenerating the checked-in output.
