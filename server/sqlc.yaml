version: "2"
sql:
  - engine: "postgresql"
    schema: "pkg/db/schema.sql"
    queries: "pkg/db/queries"
    gen:
      go:
        package: "dbgen"
        out: "pkg/db/gen"
        sql_package: "pgx/v5"
        emit_db_tags: true
        emit_interface: true
        emit_empty_slices: true
